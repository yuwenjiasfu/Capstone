1. I found that the Manifest file is `app\nRF\resources\no.nordicsemi.android.nrftoolbox.apk\AndroidManifest.xml` and the key fields include package=`no.nordicsemi.android.nrftoolbox`, versionCode=`25120939`, versionName=`4.2.0`, minSdk=`23`, targetSdk=`36`, compileSdk=`36`, application class is `no.nordicsemi.android.nrftoolbox.NrfToolboxApplication`, and is set `android:allowBackup="false"` and `android:dataExtractionRules="@xml/data_extraction_rules"`, I think these fields indicate that this App is a modern target of the Nordic official toolbox.

2. I found that the full list of permissions includes `android.permission.BLUETOOTH`(maxSdkVersion=30), `android.permission.BLUETOOTH_CONNECT`, `android.permission.POST_NOTIFICATIONS`, `android.permission.FOREGROUND_SERVICE`, `android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE`, `android.permission.RANGING`(required=false), `android.permission.ACCESS_NETWORK_STATE`, `android.permission.INTERNET`, `android.permission.WAKE_LOCK`, `android.permission.BLUETOOTH_ADMIN`(maxSdkVersion=30), `android.permission.ACCESS_COARSE_LOCATION`(maxSdkVersion=30), `android.permission.ACCESS_FINE_LOCATION`(maxSdkVersion=30), `android.permission.BLUETOOTH_SCAN`(usesPermissionFlags="neverForLocation"), `no.nordicsemi.android.LOG`, `com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE`, `com.google.android.gms.permission.AD_ID`, `android.permission.ACCESS_ADSERVICES_ATTRIBUTION`, `android.permission.ACCESS_ADSERVICES_AD_ID`, I think these permissions are related to The BLE toolbox functions match, and BLUETOOTH_SCAN explicitly declares neverForLocation. On the old system, the location permission is set to maxSdkVersion=30, which is more regular.

3. I found that the component's exposed surface is: `MainActivity` although `android:exported="true"` but only `MAIN`/`LAUNCHER` intent-filter, no BROWSABLE deep link; `no.nordicsemi.android.service.profile.ProfileService` is `android:exported="false"` with `foregroundServiceType="connectedDevice"`; `FirebaseInitProvider` is `android:exported="false"`; `androidx.startup.InitializationProvider` is `android:exported="false"`; Google Measurement/Firebase/DataTransport's service/receiver mostly exported=false or subject to system permissions; `androidx.profileinstaller.ProfileInstallReceiver` although `android:exported="true"` but with `android.permission.DUMP` permission protection, I think the risk of writing these as exported is not supported from the perspective of static component exposure.

4. I found that a large number of third-party SDK components were declared in the Manifest: `com.google.firebase.provider.FirebaseInitProvider`, `com.google.firebase.components.ComponentDiscoveryService`, Firebase Analytics connector, Firebase Crashlytics, Firebase Sessions, Firebase Installations, Firebase DataTransport, Google Play services measurement components, while setting privacy/analytics gating `firebase_analytics_collection_enabled=false`, `google_analytics_adid_collection_enabled=false`, `google_analytics_ssaid_collection_enabled=false`. I think these three make it impossible to confirm runtime collection or upload based on the existence of the SDK alone. There is obvious evidence of gating.

5. I found that the backup and migration file is in `app\nRF\resources\no.nordicsemi.android.nrftoolbox.apk\res\xml\data_extraction_rules.xml`. The evidence shows that `cloud-backup` excludes `root`, `file`, `database`, `sharedpref`, `external`, and `device-transfer` also excludes these five items. Manifest is also set to `allowBackup=false`, so I do not treat backup exposure as a strong finding here.

6. I found that the BLE connection path is in `app\nRF\sources\hf\j.java`, the method `h(String str)` receives the string address and maps it to the BLE peripheral, `n(String str, ...)` uses `LinkedHashMap` to cache the peripheral object by address, the Android 12+ branch explicitly checks the `BLUETOOTH_CONNECT` permission, I think the connection encapsulation is standard, but at the same time it is a subsequent co-located app The basis of the analysis chain.

7. I found that the BLE GATT read and write path is in `app\nRF\sources\hf\l.java`, `a()` calls `BluetoothGatt.readCharacteristic(...)`, `b(byte[] bArr, ...)` before Android 13 uses `setValue(bArr)` + `writeCharacteristic(characteristic)`, Android 13+ uses `writeCharacteristic(characteristic, bArr, writeType)`, the code will also check the write/write-no-response/signed-write type based on characteristic properties. I think this universal write package itself does not have AES/HMAC/signature wrapper, but it is a universal BLE tool library. The risk depends on the GATT protocol of the connected peripheral and the assumption that there is an application layer authentication gap.

8. I found that the BLE callback and log are implemented in `app\nRF\sources\no\nordicsemi\kotlin\ble\client\android\internal\NativeGattCallback.java`, `onCharacteristicChanged(BluetoothGatt, BluetoothGattCharacteristic, byte[])` records characteristic UUID and value, `onCharacteristicRead(BluetoothGatt, BluetoothGattCharacteristic, byte[], int)` records characteristic UUID, value, status, `onDescriptorRead(...)` includes descriptor/value processing. For the old API overload, it also takes the byte array from `bluetoothGattCharacteristic.getValue()` and gives it to the new version of the function. The log template will use `c.g(bArr, ...)` to convert the value into a printable string and hand it to the logger. I think these logs are very important for BLE health/measurement The profile may be sensitive, but without a runtime logcat or nRF Logger database export, there is no way to confirm that user device data is actually present in the logs.
