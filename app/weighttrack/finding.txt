1. I found that `SyncFitbitActivity.java:316-324` uses code to exchange token to `https://api.fitbit.com/oauth2/token`, `SyncFitbitActivity.java:336-340` parses `access_token` and then calls `g.a(..., "PARAM_FITBIT_ACCESS_TOKEN", token)`, `util/g.java:639-648` reads JWT from `PARAM_FITBIT_ACCESS_TOKEN` and determines whether to clear it based on exp. I think this is typical evidence that token stores ordinary SharedPreferences.

2. I found that `integration/withings/a/b.java:42-52` changes the URL of the Withings token through code and contains `client_secret`, and `integration/withings/a/b.java:102-108` stores the `WithingsToken` JSON when the token has not expired. `INT_WITHINGS_TOKEN`, `integration/withings/a/b.java:113-115` Read back from `INT_WITHINGS_TOKEN`; old `WithingsActivity.java:54-58` definition `WITHINGS_AUTH_TOKEN`, `WITHINGS_AUTH_SECRET`, `WITHINGS_AUTH_USER_ID`, `WithingsActivity.java:115-117` reads token/secret/userid from SharedPreferences, `WithingsActivity.java:223-226` is parsed in token response `oauth_token`, `oauth_token_secret`, I think the old path is also persisted in plain text.

3. I found that `sources/com/ikdong/weight/util/n.java:27-31` gives the external directory `/WeightTrack`, the export file `WTA.data` and the source DB `/data/com.ikdong.weight/databases/WTA.mp3`, `util/n.java:34-46` copies the private database to the directory of user choice `WTA.data`, `util/n.java:67-84` Copy the original file via `FileChannel.transferTo`, I think the original DB file copy code is fully confirmed.

4. I found that the JSON export is in `sources/com/ikdong/weight/util/f.java:22-35`, which reads the `Weight` list of `com.ikdong.weight.a.s.h()`, uses `WeightSerializer` to convert it to JSON, and then `util/f.java:38-52` is written to `/WeightTrack/weight_backup.json`; the CSV export is in `/WeightTrack/weight_backup.json` `sources/com/ikdong/weight/util/aj.java:27-43` is written to CSV under the path selected by the user. The fields are year/month/day/average weight. I think the three export paths of CSV/JSON/DB are complete.

5. I found that the evidence of sharing after exporting is `widget/fragment/b.java:263-268` sharing `Uri.fromFile(com.ikdong.weight.util.n.f2036c)` through `ACTION_SEND`, which is `WTA.data`. I think the behavior of App actively exporting/sharing sensitive data to external paths has been confirmed by the code, but it is necessary to distinguish between "App actively exporting/sharing" and "other Apps" "Necessarily readable", Android 10+ scoped storage, user authorization, and actual device policy will affect whether other apps can directly read `/WeightTrack`.

6. I found that the Manifest evidence of I-01 is: Fitbit callback `SyncFitbitActivity` exported=true, scheme/host is `wta://fitbit`, see `resources/AndroidManifest.xml:252-267`; Withings callback `SyncWithingsActivity` exported=true, scheme/host is `wta://withings`, see `resources/AndroidManifest.xml:274-289`; I think any browser or third-party App can construct `wta://fitbit?code=...` or `wta://withings?code=...` because Activity declares `BROWSABLE` and `exported=true`.

7. The code evidence I found for I-01 is: Fitbit callback processing in `sources/com/ikdong/weight/activity/SyncFitbitActivity.java:123-132` reads `getIntent().getData().getQueryParameter("code")`, if not empty, calls `e(queryParameter)`; `e(String str)` puts `code` into the POST body And with hard-coded Basic Authorization access `https://api.fitbit.com/oauth2/token`; Withings callback calls `b("code")` in `SyncWithingsActivity.java:100-102`, if not empty, `e(strB)`, calls `b.a(str, callback)` in `SyncWithingsActivity.java:307-317`; Withings token exchange in `b.a(String str, ...)` and put `code` into `https://wbsapi.withings.net/v2/oauth2?...client_secret=...&code=%s&redirect_uri=wta://withings` (see `integration/withings/a/b.java:42-52`); after success, Fitbit saves the access token to `PARAM_FITBIT_ACCESS_TOKEN`, Withings saves JSON token to `INT_WITHINGS_TOKEN`.

8. I found that the data flow of saving third-party tokens in EC-03 SharedPreferences is: the user authorizes through Fitbit or Withings, the OAuth server returns the access token/refresh token or OAuth 1 token/secret, the App hands the token to `g.a(...)`, `g.a(...)` writes to the ordinary SharedPreferences `worktrack_setting`, and during subsequent synchronization/reading, the App takes the token from SharedPreferences and puts it in `Authorization: Bearer` or OAuth signature request, I think there is no runtime file system extraction so the real token plaintext sample cannot be displayed, but the static path has confirmed that the token is stored in ordinary SharedPreferences, whether it is read by system backup, root, debug backup or device stolen depends on the device status and backup policy.

9. I found evidence of current Withings integration for EC-05 Withings plaintext HTTP and token handling: `SyncWithingsActivity` reads deep link code see `sources/com/ikdong/weight/activity/SyncWithingsActivity.java:100-102`; current authorization entry using HTTP see `sources/com/ikdong/weight/integration/withings/a/b.java:35-39`; token exchange uses HTTPS but hardcodes `client_secret` in the URL see `integration/withings/a/b.java:42-52`; token is stored in SharedPreferences `INT_WITHINGS_TOKEN` see `integration/withings/a/b.java:102-108`; measured reads use HTTPS Bearer token see `Authorization: Bearer %s` of `sources/com/ikdong/weight/integration/withings/a/a.java:26-30`, URL `https://wbsapi.withings.net/measure?...meastype=1,2...`; I think the old authorization URL of legacy `WithingsActivity.java:51` is also HTTP, the old measurement API is HTTP and the URL parameter contains OAuth See `WithingsActivity.java:112-130` for the token, userid, and HMAC signatures, see `WithingsActivity.java:128-130` for the old code printing measurement response, and see `WithingsActivity.java:218-223` for the old code response printing.

10. Full list of evidence I found for EC-08 hardcoded key/secret: Fitbit `Authorization: Basic...` see `SyncFitbitActivity.java:316-324`, Withings `client_secret=ac86a69e...` see `integration/withings/a/b.java:42-52`, old Withings OAuth consumer secret see `WithingsActivity.java:126`, Firebase URL, Google API key, Google app id, crash reporting API key, storage bucket see `resources/res/values/strings.xml:119-124`, Google web client id see `resources/res/values/strings.xml:536`, AdMob app id see `resources/AndroidManifest.xml:54-56`, Google backup api key see `resources/AndroidManifest.xml:66-68`, I think the risk of mobile API key/secret depends on whether the third-party platform treats it as confidential, whether there are package name/SHA restrictions, redirect URI restrictions, quota restrictions, the report only confirms the hard-coded exposure surface.
